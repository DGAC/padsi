# Virtual machines management

This directory contains miscellaneous scripts and programs used to set up and manage virtual machines:
- `vm-agent`: agent to be run in each VM
- `scripts`: scripts executed in VMs, depending on the OS
- `viewer`: VM viewer program

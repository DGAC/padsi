# padsi-do

Program which allows to execute programs in the context of an administration namespace, in order to use VPNs to access resources needed to manage a PADSI device. If not run as root, it relies on sudo to checck if a user is allowed to execute a program.

[package]
name = "padsi-do"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
nix = { version = "0.30.1", features = [ "mount", "sched", "user" ] }
tempfile = "3.23.0"
padsi = { path = "../padsi" }

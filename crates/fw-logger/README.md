# Firewall logger

Simple tool which gets notified of NFLog events and write them to a log file,
a very stripped down version of ULogd.

This tool is intended to be run in each zone where the firewall may log events.

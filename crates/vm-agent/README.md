# Virtual machine agent

Agent which needs to be running in a virtual machine (VM) used in the PADSI environment.
This agent is responsible to set up a VM according to some settings and offers a simple Web API to perform some tasks.

If this agent is not present, then the user experience with VMs will be less (no customization, no shared directory, ...)

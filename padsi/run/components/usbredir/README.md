# USB redirection component

This component permits programs in the Bubblewrap environment to execute the `spice-client-glib-usb-acl-helper` program to modify the ACLs of USB devices so they can be redirected to a VM via Spice.

The C implementation is only a test for now.

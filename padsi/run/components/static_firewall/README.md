# Static firewall component

This directory contains the component which add some local firewall rules

## Static firewall rules

In a file which is mapped in the bubble as `/etc/fw-rules.json`:
- the first rule which match contains the final decision, the default is **reject**
- syntax: refer to firewall/README.md

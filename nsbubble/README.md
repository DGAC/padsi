# Using BubbleWrap

The BubbleWrap tool is used here to build zones and containers like structures to host infrastructure elements.
Each **bubble* runs its own "init" process and an exterior program can use its Web API to start processes in the bubble.
